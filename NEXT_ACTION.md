# NEXT_ACTION.md

> Generated by `npm run agent:council` on 2026-05-21T00:10:23.902Z.
> The agent council picked the next task; read this before acting.

## STATUS

**SAFE_TO_PROCEED**

## Recommended next task

- **T-007** — Audit `AUDIT.md` for open items

## Why

The agent council scored T-007 highest (23.50)
on the rubric in `docs/agent-council.md`. Risk class: `docs-only`.

Last completed work (from HANDOFF.md): T-004 /validation smoke test (council-picked) (2026-05-20).

## Approval requirement

**Not required.** This task is in a safe risk class (`docs-only`). Proceed under the verification plan below.

## Exact command / prompt to run next

If you are a fresh Claude Code session, start with:

> "Run T-007 as described in TASKS.md and the council report at reports/agent-council/latest.md. Proceed under the verification plan and report when done."

## Files likely touched

AUDIT.md, BUGS.md

## Verification commands

```
npm test
npm run typecheck
npm run check:data-policy
npm run check:agent-workspace
```

## Rollback plan

- All edits should be reversible with `git checkout -- <file>` (no destructive ops without explicit approval).
- If a verification command fails after a change, revert with `git checkout -- <file>` and log the failure to `BUGS.md`.
- If the change is committed (it must not be without user approval), revert with `git revert <hash>`.
